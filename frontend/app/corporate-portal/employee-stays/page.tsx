import EmployeeStaysClient from './EmployeeStaysClient'

export const metadata = {
  title: 'Employee Stays | Corporate Portal',
  description: 'Manage employee hotel stays and payment records',
}

export default function EmployeeStaysPage() {
  return <EmployeeStaysClient />
}
